# epl-data-pipeline
Automated EPL data pipeline using API ingestion, Google BigQuery, and dbt. Raw football data is extracted, loaded into BigQuery, transformed into analytics-ready models, and stored back in BigQuery for downstream reporting.
